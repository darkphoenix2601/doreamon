from tg_bot import (DEV_USERS, SUDO_USERS, WHITELIST_USERS, TIGER_USERS, SPAMMERS, SUPPORT_USERS, telethn)

IMMUNE_USERS = DEV_USERS + SUDO_USERS + WHITELIST_USERS + TIGER_USERS + SPAMMERS + SUPPORT_USERS

IMMUNE_USERS = list(SUDO_USERS) + list(WHITELIST_USERS) + list(SUPPORT_USERS) + list(
    TIGER_USERS) + list(DEV_USERS) + list(SPAMMERS)
